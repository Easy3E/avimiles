import {redis,botOK} from "../common"; export async function GET(req){if(!botOK(req))return Response.json({error:"Unauthorized"},{status:401});const r=redis(),codes=await r.smembers("verified-links"),items=[];for(const code of codes){const item=await r.get(`link:${code}`);if(!item){await r.srem("verified-links",code);continue;}if(item.status==="verified")items.push({code,...item});}return Response.json({items});}